export const LIGHT_THEME = {
  body: "red",
  primaryBackground: "blue",
};

export const DARK_THEME = {
  body: "#17223b",
  primaryBackground: "white",
};

export type ThemeType = typeof LIGHT_THEME; // check globalStyles.ts

